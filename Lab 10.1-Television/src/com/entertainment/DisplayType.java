package com.entertainment;/*
 * These are com.entertainment.DisplayType objects (NOT Strings).
 * These are the ONLY 5 DisplayTypes objects possible - a fixed set of named instances.
 * Referenced as com.entertainment.DisplayType
 */

public enum DisplayType {
    LED, OLED, PLASMA, LCD, CRT
}